# Library Management System (Java Console App)

## 📌 Overview
This is a simple **Library Management System** built in Java.  
It allows users to:
- View available books
- Borrow books
- Return books  

The project demonstrates **Object-Oriented Programming (OOP) concepts** such as:
- Classes & Objects  
- Encapsulation  
- Lists and Iteration  
- Basic user input handling  

---

## 🛠️ Tech Stack
- Java (Core)
- OOP concepts
- Console-based interaction

---

## 🚀 Features
- Add books to the library
- Display all books with status (Available / Borrowed)
- Borrow a book (changes status)
- Return a book (restores availability)
- Simple text-based menu system

---

## ▶️ How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/library-management-system.git
Open the project in your Java IDE (IntelliJ, Eclipse, VS Code).

Compile and run the LibraryApp.java file.

Use the menu options to interact with the library.
